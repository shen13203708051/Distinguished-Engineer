<button disabled = "{{!canAdd}}" bindtap="add">Add</button>
<button disabled = "{{!canSub}}" bindtap="sub">Sub</button>
<view wx:for="{{Array}}" wx:for-index="idx" wx:for-item="itemName">
  {{idx+1}} '{{itemName}}'
</view>
