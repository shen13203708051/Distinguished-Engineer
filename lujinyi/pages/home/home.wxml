<!--pages/home/home.wxml-->
<button disabled="{{disabledNum >= 26}}" bindtap="add"> 加 </button>
<button disabled="{{disabledNum <= 7}}" bindtap="minus"> 减 </button>

<view wx:for="{{array}}">
  {{index+1}}: '{{item}}'
</view>
